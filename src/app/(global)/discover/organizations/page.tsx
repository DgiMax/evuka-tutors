import OrgDiscoveryClient from "@/components/org/OrgDiscoveryClient";

export const metadata = {
  title: "Discover Organizations",
  description: "Browse and join communities on our platform.",
};

export default function OrgDiscoveryPage() {
  return <OrgDiscoveryClient />;
}